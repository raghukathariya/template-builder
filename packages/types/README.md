# @template-builder/types

Shared domain TypeScript types (Template, Block, Variable, Theme, Version, Asset, Folder, User, Role, Permission) — pure type-only package, no runtime code, consumed by both apps/api and apps/web.
