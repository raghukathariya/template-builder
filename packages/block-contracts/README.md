# @template-builder/block-contracts

Framework-agnostic interfaces for the block system (IBlockDefinition, IBlockRenderer, IBlockEditor, IBlockValidator, IPropertyEditor). No React or Fastify imports — implemented by apps/web/src/blocks on the client and apps/api/src/modules/blocks on the server.
