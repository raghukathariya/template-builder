# @template-builder/validation

Shared Zod schemas backing the Variable Engine and Validation Engine. Same schema definitions drive backend DTO validation and frontend React Hook Form validation — one validation engine, not two.
